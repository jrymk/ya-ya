[global]

lay_unfertilized_egg
    position: argCoord[0]

lay_fertilized_egg
    position: argCoord[0]
    genderIsMale: argBool[0]

hatch
    position: argCoord[0]
    genderIsMale: argBool[0]

destroy
    id: argString[0]

process_collision
    e: argEntity[0]
    f: argEntity[0]

find_mate_female
    e: argEntity[0]

lay_egg_find_nearby_male
    e: argEntity[0]

[duck]

init
loop_wander
loop_lay_eggs
loop_find_mate
death
hop

have_sex_with
    partner: argEntity[0]

duckwalk_to_until
    target: argCoord[0]

slide_velocity_distance
    velocity: argCoord[0]
    distance: argFloat[0]

lay_unfertilized_egg
lay_fertilized_egg

slide_instant
    delta: argCoord[0]

slide_velocity
    velocity: argCoord[0]
    
result_find_mate_female
    partner: argEntity[0]
    position: argCoord[0]

[egg]

init
hatch
hop
